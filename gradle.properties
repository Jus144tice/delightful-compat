org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

# --- Project coordinates (kept here so build.gradle stays declarative) ---
modId=delightful_compat
modVersion=1.3.2
modGroup=com.delightfulcompat
modLicense=Apache-2.0

# --- Platform versions (mirror the proven minecraft-manager/agent-neoforge setup) ---
minecraftVersion=1.21.1
neoForgeVersion=21.1.233

# --- Optional integration: JEI for NeoForge 1.21.1 (compile-only API + dev runtime) ---
jeiVersion=19.27.0.340
